backend:
	name: git-gateway
	repo: nk7788/website_test


media_folder: "assets/img"

collections:
  - name: post
    label: Post
    folder: posts
    create: true
    fields:
      - {name: title, label: Title}
      - {name: body, label: Body, widget: markdown}
